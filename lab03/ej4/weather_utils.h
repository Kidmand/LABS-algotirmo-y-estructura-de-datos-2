#include <stdbool.h>

int minima_historica(WeatherTable a);

void mayor_temp_ano(WeatherTable a, int output[YEARS]);

void maxima_prec_mes_ano(WeatherTable a, int output[YEARS]);

void imprimir(int outout[YEARS]);

